Implementation Notes:
- The "Hello World" values comes from a backend endpoint
- The backend gets its data from the in-memory database
- The in-memory database has a table called temp that holds the "Hello World" value
