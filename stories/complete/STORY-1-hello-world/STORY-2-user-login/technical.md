Implementation Notes:
- Once the user inputs a username and submits, a profile created in the database using that username
- If the username already exists, use that profile record.  Do not create another
- Profiles should be created and retreived via service endpoints
